# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The project's facts (purpose, version matrix, build, layout, code patterns, conventions, commands and known test
failures) live in `STATE.md`, which is shared with other assistants (Junie reads it via `.junie/guidelines.md`).
Keep them there rather than here, and update `STATE.md` in the same PR when a change makes it stale.

@STATE.md
